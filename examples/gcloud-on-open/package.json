{
  "name": "@rigkit/example-gcloud-on-open",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "rig:plan": "rig run plan",
    "rig:apply": "rig run apply",
    "rig:fork": "rig run create --name gcloud-workspace",
    "rig:ssh": "rig run gcloud-workspace/ssh",
    "rig:rm": "rig run gcloud-workspace/remove -y",
    "typecheck": "tsc --noEmit",
    "test": "echo no gcloud on-open example tests yet"
  },
  "dependencies": {
    "@rigkit/provider-freestyle": "workspace:*",
    "@rigkit/provider-gcloud-cli": "workspace:*",
    "@rigkit/sdk": "workspace:*",
    "freestyle": "^0.1.51"
  },
  "devDependencies": {
    "@rigkit/cli": "workspace:*",
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
