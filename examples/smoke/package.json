{
  "name": "@rigkit/example-smoke",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "rig:plan": "rig run plan",
    "rig:apply": "rig run apply",
    "rig:fork": "rig run create --name smoke-workspace",
    "rig:ssh": "rig run ssh smoke-workspace",
    "rig:rm": "rig run smoke-workspace/remove -y",
    "typecheck": "tsc --noEmit",
    "test": "echo no smoke example tests yet"
  },
  "dependencies": {
    "@rigkit/provider-freestyle": "workspace:*",
    "@rigkit/sdk": "workspace:*",
    "freestyle": "^0.1.51"
  },
  "devDependencies": {
    "@rigkit/cli": "workspace:*",
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
