{
  "name": "@freestyle-sh/fdev-example-smoke",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fdev:plan": "fdev run plan",
    "fdev:apply": "fdev run apply",
    "fdev:fork": "fdev run create --name smoke-workspace",
    "fdev:ssh": "fdev run ssh smoke-workspace",
    "fdev:rm": "fdev run delete smoke-workspace -y",
    "typecheck": "tsc --noEmit",
    "test": "echo no smoke example tests yet"
  },
  "dependencies": {
    "@freestyle-sh/fdev-provider-freestyle": "workspace:*",
    "@freestyle-sh/fdev": "workspace:*"
  },
  "devDependencies": {
    "@freestyle-sh/fdev-cli": "workspace:*",
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
