{
  "name": "@rigkit/example-smoke",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "rig:plan": "rig plan",
    "rig:apply": "rig apply",
    "rig:fork": "rig create --name smoke-workspace",
    "rig:ssh": "rig ssh smoke-workspace",
    "rig:rm": "rig delete smoke-workspace -y",
    "typecheck": "tsc --noEmit",
    "test": "echo no smoke example tests yet"
  },
  "dependencies": {
    "@rigkit/provider-freestyle": "workspace:*",
    "@rigkit/sdk": "workspace:*"
  },
  "devDependencies": {
    "@rigkit/cli": "workspace:*",
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
