# @freestyle/fdev-sdk

Authoring SDK for `fdev.config.ts`.

```ts
import { defineDevMachine, defineMigration, env } from "@freestyle/fdev-sdk";
```

This package provides config helpers and public types. Runtime execution lives in `@freestyle/fdev-engine`.
