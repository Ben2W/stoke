# @freestyle-sh/fdev-provider-freestyle

Freestyle provider integration for `fdev`.

This package supplies:

- `defineFreestyleProvider(...)` for `fdev.config.ts`
- `createFreestyleProvider(...)` for the base fdev provider contract
- Freestyle-specific SQLite schema and store helpers
