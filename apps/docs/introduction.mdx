---
title: "Rigkit"
description: "Declarative dev environments, in TypeScript."
---

Rigkit runs development environments from a TypeScript config.

You write the setup once, Rigkit prepares it, and then you create as many named
workspaces as you need for agents, developers, CI jobs, or tests.

## Core loop

```sh
curl -fsSL https://www.rigkit.dev/install | sh
```

Create a project and bring up your first workspace:

```sh
mkdir website
cd website
pnpm add -D @rigkit/sdk @rigkit/provider-freestyle @rigkit/provider-cmux freestyle
rig init
rig plan
rig apply
rig create dev
rig run dev ssh
rig rm dev
```

- `rig plan` shows which setup tasks are cached or pending.
- `rig apply` runs pending tasks and stores their JSON `ctx`.
- `rig create <name>` creates a workspace from the workflow output.
- `rig run <name> <operation>` runs an operation defined by `rigkit/index.ts`.

## Read next

- [Quickstart](/guides/quickstart): install, initialize, apply, and create a workspace.
- [CLI reference](/reference/cli): command syntax and global options.
- [Workflows](/guides/workflows): tasks, context, composition, config, and caching.
- [Workspaces and operations](/guides/workspaces): workspace lifecycle hooks and `rig run`.
- [Provider overview](/providers/overview): built-in providers and provider design rules.

Runnable examples live in the
[Rigkit repository](https://github.com/freestyle-sh/rigkit/tree/main/examples).
